# Test Environment Variables
# This file is used by Playwright E2E tests

# Database (local Docker Postgres)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/food_scanner

# Session (iron-session password, min 32 characters)
SESSION_SECRET=test-session-secret-min-32-chars-required-for-iron-session

# App URL (production build on port 3001)
APP_URL=http://localhost:3001

# Auth
ALLOWED_EMAILS=test@example.com

# Google OAuth (test values - not used in E2E)
GOOGLE_CLIENT_ID=test-client-id
GOOGLE_CLIENT_SECRET=test-client-secret

# Anthropic (test key - not used in smoke tests)
ANTHROPIC_API_KEY=test-anthropic-key

# Fitbit (dry-run mode for tests)
FITBIT_DRY_RUN=true

# Test-only auth bypass
ENABLE_TEST_AUTH=true

# Server port (avoid conflict with dev server)
PORT=3001
