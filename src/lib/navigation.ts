/**
 * Navigation tab paths in display order.
 * Used by BottomNav and swipe navigation hook.
 */
export const TAB_PATHS = [
  "/app",
  "/app/history",
  "/app/analyze",
  "/app/quick-select",
  "/app/chat",
] as const;
