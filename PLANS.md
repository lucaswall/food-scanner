# Implementation Plan

No active plan.

This file is written by the planning skills (`plan-backlog`, `plan-inline`, `plan-fix`) and
consumed by `plan-implement` / `plan-review-implementation`. It is reset to this state once a
plan is released.
